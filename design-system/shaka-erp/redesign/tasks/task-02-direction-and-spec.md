# Task 2: Direction and spec (no theme code)

> Before starting: read `../PLAN.md` (Context, How every task runs, House rules). When done: tick this task in `../PLAN.md`, then commit.

- **Baseline:** screenshot the current look in the matrix into `.impeccable/review/baseline/`: home menu, command palette, a list, a form with chatter and statusbar, kanban, a dialog, a dropdown, settings, Discuss, login, and the Budget Plan and Category Manager screens.
- **ui-ux-pro-max:** run `search.py "enterprise ERP finance BI apple macOS system settings" --design-system -p "Shaka ERP" --variance 3 --motion 4 --density 7 --format markdown`, **without `--persist`**. Add `--domain ux "data table density"`, `--domain color "apple system colors"` and `--domain typography "system font"`. Treat all of it as raw input.
- **apple-design** rules for an ERP:
  - §1 press feedback.
  - §4 critically damped springs.
  - §7 menus grow from their trigger.
  - §12 materials only on floating layers, plus scroll-edge effects.
  - §14 reduced motion, transparency and contrast.
  - §15 hierarchy from weight, size and leading.
- **impeccable new-work** (replacement world, Operate mode): run the direction round (`concept-seed` plus the decision page) with the 4 decisions **pinned**. Write the direction contract into the `addons/shaka_theme` surface brief.
- **Rewrite `design-system/shaka-erp/MASTER.md`** for the new world, keeping its header, structure and checklist:
  - **Color table** (light/dark, **measured contrast per pair**). Starting point, to verify:
    - Action-blue fill `#0071E3`, same in both schemes (white text ≈4.7:1).
    - Link `#0066CC` / `#2997FF`.
    - Grouped background `#F5F5F7` / `#1C1C1E`; surface `#FFFFFF` / `#2C2C2E`.
    - Label `#1D1D1F` / `#F5F5F7`; secondary label `#6E6E73` / `#98989D`.
    - Separator `#D2D2D7` / `#38383A`; fill `rgba(118,118,128,.12)` / `.24`.
    - Status: Apple's accessible variants (green `#248A3D`, orange `#C93400`, red `#D70015`), lighter in dark.
    - Gold `#B5925F` for decoration only, plus a deeper gold for icons that must reach 3:1.
  - **Type scale:** 12/13/14/17/20/24 with weights and leading.
  - **Layout:** 4pt spacing, 32px rows.
  - **Radii:** 6 controls, 10 cards/menus, 14 dialogs, capsules, plus `corner-shape: squircle` under `@supports`.
  - **Depth:** 3 shadows; thin/regular/thick materials.
  - **Motion:** tokens as CSS `linear()` springs **generated by a small script** from damping and response. Existing `--acc-*`, `--morph-*`, `--modal-*` and `--tabs-*` names stay.
  - **Components:** one spec per surface task (4–13).
  - **Anti-patterns:** updated so glass is allowed only on floating layers.
- Done when you've picked the direction on the decision page and every MASTER token has its contrast numbers. Commit.
