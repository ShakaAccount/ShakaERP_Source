# Task 12: Settings → `surfaces/90_settings.scss`

> Before starting: read `../PLAN.md` (Context, How every task runs, House rules). When done: tick this task in `../PLAN.md`, then commit.

- macOS **System Settings.** Left app list with squircle icons; the selected app is an accent-filled rounded row. Right pane: grouped inset sections with headers above, hairline rows, and Task 4 controls. The search field is a Spotlight capsule.
- Hooks: `--settings__tab-bg(--active)`, `--settings__tab-color` and `--settings__title-bg`, plus `--SearchableSetting__highlight-*`.
