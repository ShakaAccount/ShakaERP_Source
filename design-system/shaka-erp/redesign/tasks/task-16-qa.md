# Task 16: QA (`/impeccable audit` + `/impeccable critique`)

> Before starting: read `../PLAN.md` (Context, How every task runs, House rules). When done: tick this task in `../PLAN.md`, then commit.

- **Audit:**
  - Measured contrast in both schemes.
  - Keyboard-only pass through a form, a list, a dialog and the command palette.
  - `title` / `aria-label` on icon-only buttons.
  - Backdrop-filter paint cost.
  - No horizontal scroll at 1440/1024/768/390.
  - Reduced motion, transparency and contrast.
- **Critique:** home menu, list, form and kanban against the baseline and the direction contract.
- **Out-of-scope smoke check:** an invoice PDF, the portal/website if installed, POS if installed.
- One fix batch, one confirm round.
